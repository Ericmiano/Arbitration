import { StubScreen } from '../components/StubScreen';

export function DocumentRegister() {
  return (
    <StubScreen
      title="Document register"
      body="The full case file across all arbitrations: filename, category, version, uploader, date, and who is authorised to see each document. Documents are already available per case - open a case's Documents to upload or view them."
    />
  );
}
